[pytest]
markers =
    unit: Unit tests with mocked dependencies (fast, no network required)
    integration_api: Integration tests that call real MARRVEL API (requires network)
    integration_mcp: Integration tests that run MCP server (requires server startup)
    integration: Generic integration test marker (deprecated - use integration_api or integration_mcp)
addopts = -ra

# Test execution examples:
# pytest -m unit                              # Run only fast unit tests
# pytest -m integration_api                   # Run only API integration tests
# pytest -m integration_mcp                   # Run only MCP server integration tests
# pytest -m "integration_api or integration_mcp"  # Run all integration tests
# pytest -m "not integration_api and not integration_mcp"  # Run unit tests only
